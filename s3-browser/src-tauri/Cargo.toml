[package]
name = "s3-browser-desktop"
version = "0.1.0"
description = "S3 Browser desktop shell"
authors = ["S3 Browser"]
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }

[profile.release]
panic = "abort"
codegen-units = 1
lto = true
strip = true
